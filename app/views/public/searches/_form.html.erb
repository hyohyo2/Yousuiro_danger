<% if user_signed_in? %>
  <%= form_with url: search_path, method: :get, local: true do |f| %>
    <%= f.text_field :content %>
    <%= f.select :model, options_for_select({"ユーザー名" => "user", "投稿住所" => "post" }) %>
    <%= f.submit "検索" %>
  <% end %>
<% end %>